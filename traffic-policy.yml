on_http_request:
  - actions:
      - type: forward-internal
        config:
          url: https://my-backend.internal
          on_error: continue
      - type: custom-response
        config:
          status_code: 404
          content: "Not Found"
          headers:
            content-type: "text/plain"